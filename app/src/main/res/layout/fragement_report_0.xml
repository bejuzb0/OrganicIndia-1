<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <RelativeLayout
        android:layout_height="match_parent"
        android:paddingHorizontal="10dp"
        android:layout_width="match_parent">

        <TextView
            android:id="@+id/Revenue_Amount_text"
            android:text="Revenue and Amount"
            android:layout_centerHorizontal="true"
            android:layout_width="wrap_content"
            android:textColor="@color/colorAccent"
            android:layout_height="wrap_content">
        </TextView>
        <androidx.recyclerview.widget.RecyclerView
            android:layout_below="@+id/Revenue_Amount_text"
            android:id="@+id/Revenue_Amount_recycler"
            android:layout_width ="match_parent"
            android:layout_marginTop="5dp"
            android:layout_height="130dp">
        </androidx.recyclerview.widget.RecyclerView>

        <TextView
            android:text="No Delivery Reports"
            android:layout_below="@+id/Revenue_Amount_recycler"
            android:layout_centerHorizontal="true"
            android:id="@+id/no_delivery_text"
            android:textColor="@color/colorAccent"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content">
        </TextView>

        <androidx.recyclerview.widget.RecyclerView
            android:layout_marginTop="5dp"
            android:layout_below="@+id/no_delivery_text"
            android:id="@+id/no_delivery_recycler"
            android:layout_width="match_parent"
            android:layout_height="match_parent">
        </androidx.recyclerview.widget.RecyclerView>
    </RelativeLayout>

</androidx.constraintlayout.widget.ConstraintLayout>